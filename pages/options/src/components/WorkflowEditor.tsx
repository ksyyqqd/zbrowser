/**
 * Workflow Editor — public entry.
 * The full implementation lives under `./workflow/`. This file re-exports the
 * default export to keep the original import path stable for upstream callers
 * (e.g. WorkflowSettings.tsx).
 */
export { default } from './workflow';
